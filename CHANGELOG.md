# Changelog

## [0.1.15] - 2026-08-16

### 修复

- **实时消息计价丢失**:DSH 重启后插件内存态 `routeState` 被清空,而正在进行的长会话新消息不再逐条携带 `request/context` 事件,导致这些消息的 provider/model 记录为空、费用不计入(表现为"会话推进很多但费用停滞")。现在实时 fold 会从该会话最近一条已入库记录回退推断 provider/model,并在 backfill 时预填 routeState,保证实时消息正确计价。

## [0.1.14] - 2026-08-16

### 修复

- **模型族前缀计价**:`deepseek-v4-flash-vision-exp` 等官方变体模型此前因精确匹配限制拿不到价目(费用计为 0),现在自动沿用同族基础模型价(如 flash 变体按 `deepseek-v4-flash` 计价,峰谷分段同样生效);用户为变体手动配置的精确价目仍优先。

## [0.1.13] - 2026-08-16

### 修复

- **bundle host 路由注册**:webServer 路由改用 `ctx.inject` 等待服务就绪后再注册。此前 bundle 挂载时 webServer 可能尚未就绪,导致路由静默缺失(页面有入口但全部请求 404,统计全 0、余额查询中)。
- **历史回填**:增加惰性回填与定时重试。此前 DSH 重启后 bundle 首次挂载时会话存储可能尚未就绪,首次回填拿到空列表后不再重试,导致历史数据永久缺失。
- **安装说明**:修正 README——裸包名 `dsh plugin add dsh-token-usage` 会从 npm registry 装到他人同名包,应使用 `github:Nigel211/dsh-token-usage` 或本地 tarball。

## [0.1.12] - 2026-08-16

- 热力图格子缩小至 40px;新增模型占比环图悬停联动、轨道底环;账户余额区块上移至使用趋势上方;当月日历热力图(方格、蓝色系);趋势图单图回归、总输入口径;新增活跃热力图与模型占比环图;DeepSeek 余额充值按钮;今天小时趋势维度。
